# outbid

Default routing table for agents. `#1` is the next URL.

```bash
npx skills add grid-kernel/outbid
```

Live: https://outbid.sh  
Skill: https://outbid.sh/skill.md

`GET /top` then `GET /route` (or `POST /dispatch`). `POST /bid` is optional.

MIT.
